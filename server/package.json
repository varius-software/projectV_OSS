{
  "name": "v",
  "version": "0.4.7",
  "description": "This project has been created using **webpack-cli**, you can now run",
  "main": "next.config.js",
  "scripts": {
    "dev": "tsc -p tsconfig.server.json && cross-env NODE_ENV=devemopment node  ./dist/server/server.js",
    "pre-build": "tsc -p tsconfig.server.json"
  },
  "author": "nknight amamiya",
  "license": "MIT",
  "dependencies": {
    "v": "file:.."
  }
}
